import styled from 'styled-components'
import { Layout as Lay } from 'antd'

export const Content = styled(Lay.Content)`
  margin: 0 auto !important;
  width: 50% !important;
  padding: 50px 0px;
`
export const Layout = styled(Lay)`
  min-height: 100vh !important;
`
